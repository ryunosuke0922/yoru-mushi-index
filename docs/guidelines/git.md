# Git Guidelines

## Branch

- 作業ブランチは `codex/` prefix を基本にする
- 目的外の変更を混ぜない

## Commit

コミットメッセージは短く、変更の目的が分かるものにする。

Examples:

```txt
Add weekly forecast API
Document location policy
Refactor forecast dashboard components
```

## Pull Request

PR には以下を含める。

- 変更概要
- 画面/API への影響
- 検証コマンド
- 未対応事項
